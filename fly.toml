app = "zeynal-bot-core"
primary_region = "fra"

[env]

[processes]
bot = "python bot.py"

[build]
  builder = "paketobuildpacks/builder:base"

[[services]]
  internal_port = 8080
  processes = ["bot"]
