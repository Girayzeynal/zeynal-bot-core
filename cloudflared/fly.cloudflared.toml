app = "hoopbrain-cloudflared"

kill_signal = "SIGINT"
kill_timeout = 5

[build]
  dockerfile = "cloudflared/Dockerfile"

[processes]
  app = "cloudflared"

[env]
  TUNNEL_TOKEN = "TUNNEL_TOKEN_GELECEK"

[[services]]
  internal_port = 8080
  protocol = "tcp"
  processes = ["app"]
